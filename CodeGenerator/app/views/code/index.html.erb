<div class="genSection">
	<h1>CODE GENERATOR</h1>
	<% if flash[:code] %>
		<p id="success"> <%= flash[:success] %> </p>
		<div id="random-code"><%= flash[:code] %></div>
	<% end %>
	<%= button_to 'Generate new code', code_index_path, :class => "button", :method => :post %>
</div>

<div class="usedSection">
	<h2>Codes Used(<%= @codes.length %>)</h2>
	<div class="usedCodes">
		<% @codes.each do |x| %>
			<div class="usedCode"><%= x.code %></div>
		<% end %>
	</div>
</div>